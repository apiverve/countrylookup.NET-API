<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.CountryLookup</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/countrylookup</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Country Lookup is a simple tool for looking up country data. It returns the country name, capital, and more.</Description>
    <RepositoryUrl>https://github.com/apiverve/countrylookup-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>country lookup; country data; country lookup api; country data api; country lookup tool; country data tool; country lookup software; country data software; country lookup service; country data service; country lookup library; country data library; country lookup sdk; country data sdk; country lookup code; country data code; country lookup script; country data script; country lookup program; country data program; country lookup application; country data application; country lookup app; country data app; country lookup tool; country data tool; country lookup utility; country data utility; country lookup helper; country data helper; country lookup helper tool; country data helper tool; country lookup helper software; country data helper software; country lookup helper service; country data helper service; country lookup helper library; country data helper library; country lookup helper sdk; country data helper sdk; country lookup helper code; country data helper code; country lookup helper script; country data helper script; country lookup helper program; country data helper program; country lookup helper application; country data helper application; country lookup helper app; country data helper app; country lookup helper tool; country data helper tool; country lookup helper utility; country data helper utility; country lookup helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
